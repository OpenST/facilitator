{
  "name": "mosaic-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ mosaic/origin-erc20gateway",
    "create-local": "graph create --node http://localhost:8020/ mosaic/origin-erc20gateway",
    "remove-local": "graph remove --node http://localhost:8020/ mosaic/origin-erc20gateway",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mosaic/origin-erc20gateway"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.16.1",
    "@graphprotocol/graph-ts": "0.16.0"
  }
}
